apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: ${WEBSHOP_ACTIVITIES_TABLE}
    user: airflow
    secureJsonData:
      password: airflow
    jsonData:
      sslmode: "disable"